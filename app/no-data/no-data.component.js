angular.
  module('noData').
  component('noData', {
    templateUrl: 'no-data/no-data.template.html',
    controller: ['Data',
    function noDataController(Data) {

      var self = this;


    }
  ]
});
